import React from "react";
import SigninButton from "@/components/SigninButton";

const MenuBar = () => {
    return (
    <header>
        <SigninButton />
    </header>
    )
}

export default MenuBar;
